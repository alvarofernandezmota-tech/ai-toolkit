# 📓 Diario — 22 Abril 2026

> Sesión: tarde-noche · Duración: ~2h · Herramienta: Perplexity + GitHub MCP

---

## ✅ Qué se hizo

### Auditoría y fixes del repo
- **E1 — Fix `start-colmena.sh`**: puerto 8000 ocupado por uvicorn de THDORA → añadido `lsof -ti:8000 | xargs kill -9` al arranque
- **E8 — Fix `.gitignore`**: `.env` y archivos sensibles no estaban excluidos → corregido
- Script nuevo **`scripts/ai-menu.sh`** (S16): menú interactivo con 12 opciones, punto de entrada principal del toolkit

### Documentación actualizada
| Archivo | Qué cambió |
|---|---|
| `README.md` | `ai-menu.sh` como punto de entrada, próximos pasos al día |
| `ECOSISTEMA.md` | Estado APIs real, `ai-menu.sh` + `ensemble.sh` documentados |
| `INICIO-AQUI.md` | Tabla de estado con todos los scripts nuevos |
| `agentes/PENDIENTES.md` | 8 completados con fecha, backlog actualizado |
| `CHANGELOG.md` | Sesión completa documentada |
| `ROADMAP.md` | Items completados movidos al historial |
| `docs/diario/2026-04-22.md` | Este archivo |

---

## 🔴 Pendiente para próxima sesión

1. **Renovar key Groq** → [console.groq.com/keys](https://console.groq.com/keys) → actualizar `~/.bashrc`
2. **Renovar key DeepSeek** → [platform.deepseek.com](https://platform.deepseek.com) → actualizar `~/.bashrc`
3. **Primer uso real de Claude Code en THDORA** → atacar los 4 bugs confirmados en producción
4. **Ejecutar `bash scripts/benchmark-runner.sh`** → ~20 min, script listo

---

## 🧠 Aprendizajes del día

- El fix de `start-colmena.sh` era sencillo pero bloqueante: uvicorn de THDORA ocupaba el 8000 y LiteLLM no podía arrancar
- `ai-menu.sh` simplifica el onboarding enormemente — un solo comando para todo el stack
- La documentación estaba desincronizada ~5 días respecto al código real
- Workflow con Perplexity + GitHub MCP para pushear directo funciona muy bien para sesiones de documentación

---

## 📊 Estado del repo al cierre

```
✅ LiteLLM proxy    → operativo
✅ OpenCode         → operativo
✅ Ollama           → operativo (qwen3:8b, qwen2.5-coder:14b, deepseek-r1:14b)
✅ Cerebras         → operativo
✅ OpenRouter       → operativo
✅ Google Gemini    → operativo
⚠️ Groq             → key caducada
⚠️ DeepSeek API     → key caducada
```

Commits hoy: **7 pushes** · Archivos modificados: **9**
