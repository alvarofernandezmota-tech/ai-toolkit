# Sesión 2026-04-18 — Madrugada

## Resumen
Primera sesión con OpenCode completamente operativo conectado a LiteLLM proxy.
Primera llamada real end-to-end confirmada: OpenCode → LiteLLM :8000 → Cerebras → 200 OK.

## Stack al inicio de sesión
| Componente | Estado | Notas |
|---|---|---|
| Ollama local | ✅ operativo | qwen2.5-coder:14b, deepseek-r1:14b, qwen3:8b |
| LiteLLM proxy | ✅ operativo | Puerto :8000 |
| OpenCode TUI | ✅ operativo | Fix `autosave` aplicado sesión anterior |
| Cerebras | ✅ operativo | Proveedor principal confirmado |
| Gemini | ❌ key caducada | Renovar en aistudio.google.com/apikey |
| Groq | ❌ key caducada | Renovar en console.groq.com |

## Lo que se hizo

### 1. Verificación del repo (auditoría)
- Confirmado estado del repo en commit `60e763a`
- Identificados archivos residuales: `opencode.json.tui-migration.bak`, `tui.json` (91 bytes)
- Duplicado detectado: `docs/SESION-2026-04-17.md` vs `docs/sesion-2026-04-17.md`
- Doble ubicación de diario: `diario/` en raíz y `docs/diario/`

### 2. Primera llamada real OpenCode → LiteLLM
- Prompt enviado: `Revisa el fichero litellm-config.yaml y dime si hay algún modelo mal configurado`
- Proveedor usado: `Principal (Cerebras → OpenRouter)`
- Resultado: `POST /v1/chat/completions HTTP/1.1 200 OK` ✅
- Stack end-to-end validado por primera vez

### 3. Limpieza del repo (esta sesión)
- Eliminado `opencode.json.tui-migration.bak`
- Eliminado duplicado `docs/SESION-2026-04-17.md` (conservado `docs/sesion-2026-04-17.md`)
- Creado `docs/sesion-2026-04-18.md` (este fichero)

## Pendiente
- [ ] Renovar `GOOGLE_GENERATIVE_AI_API_KEY` → aistudio.google.com/apikey → `.env`
- [ ] Renovar `GROQ_API_KEY` → console.groq.com → `.env`
- [ ] Verificar si `tui.json` (91 bytes) sigue siendo necesario
- [ ] Consolidar `diario/` raíz en `docs/diario/`
- [ ] Push manual de CHANGELOG.md + litellm-config.yaml si quedan cambios locales sin push

## Ficheros clave
```
~/ai-toolkit/litellm-config.yaml      # Config proxy LiteLLM
~/.config/opencode/opencode.json      # Config OpenCode (o ~/ai-toolkit/opencode.json)
~/ai-toolkit/.env                     # Variables de entorno (keys API)
~/ai-toolkit/scripts/start-colmena.sh # Arrancar todo el stack
```

## Arrancar stack
```bash
bash ~/ai-toolkit/scripts/start-colmena.sh
# Luego en otra terminal:
opencode
```

## Modelos disponibles en LiteLLM
- `principal` → Cerebras → OpenRouter (fallback)
- `ollama-fallback` → Ollama local
- `cerebras-fallback`, `openrouter-fallback`
- `gemini-fallback`, `gemini-lite-fallback` ❌ key caducada
- `groq-fallback` ❌ key caducada
- `sambanova-*`, `together-*`, `llama-4-*`, `qwen3-235b`, `mistral-large`, `codestral`
