# Sesión 2026-04-17 — Estado del Ecosistema

## ✅ Lo que funciona ahora mismo

| Componente | Estado | Detalle |
|---|---|---|
| LiteLLM Colmena | ✅ | Corriendo en `:8000` |
| Ollama | ✅ | qwen2.5-coder:14b respondiendo |
| OpenCode | ✅ | Instalado en `~/.npm-global/bin/opencode` |
| Groq | ✅ | llama-3.3-70b-versatile operativo |
| Sambanova | ✅ | Llama-4-Maverick operativo |
| Together AI | ✅ | Llama-4-Maverick operativo |
| Gemini | ❌ | Cuota free tier agotada — al final del fallback siempre |

## 🧠 Modelos locales Ollama

```
qwen3:8b-q4_K_M       5.2 GB   (reasoning)
qwen2.5-coder:7b      4.7 GB   (código rápido)
qwen2.5-coder:14b     ~9 GB    (código calidad — PRINCIPAL local)
```

## 🔧 Fix aplicado esta sesión

- `request_timeout: 15 → 120` — Ollama tarda ~6 min primera carga en RAM
- Ollama añadido como **primera opción** en todos los fallbacks (coste 0)
- PATH de opencode añadido al `.bashrc`

## 🚀 OpenCode — cómo arrancarlo

```bash
# 1. Asegúrate de que LiteLLM está corriendo
bash ~/projects/ai-toolkit/scripts/start-colmena.sh

# 2. En otro panel tmux, lanzar OpenCode en el proyecto
cd ~/projects/ai-toolkit
opencode
# o directamente:
~/.npm-global/bin/opencode
```

## 🎯 Primera tarea para OpenCode (pendiente)

Darle el ROADMAP y que construya `scripts/generar-diario.sh`:

```
Lee el archivo ROADMAP.md y crea el script scripts/generar-diario.sh
que genere el brief diario automaticamente.
```

## 📐 Arquitectura actual

```
┌─────────────────────────────────────────────────────┐
│                   OpenCode (agente)                  │
│              ~/.npm-global/bin/opencode               │
└─────────────────────┬───────────────────────────────┘
                      │ HTTP :8000
┌─────────────────────▼───────────────────────────────┐
│              LiteLLM Proxy (Colmena)                  │
│                  localhost:8000                       │
│  routing: least-busy | timeout: 120s | retries: 1    │
└──┬──────────┬──────────┬──────────┬──────────┬───────┘
   │          │          │          │          │
   ▼          ▼          ▼          ▼          ▼
Ollama     Groq      Sambanova  Together   OpenRouter
(local)  (6k/día)   (gratis)   ($1 cr.)   (gratis)
qwen     llama3.3   llama4     llama4     llama4-scout
```

## 📝 Próximos pasos

1. `git pull` en el Acer para tener el config actualizado
2. `bash scripts/start-colmena.sh` — reiniciar con timeout 120s
3. Lanzar `opencode` y darle la primera tarea: leer ROADMAP + crear generar-diario.sh
4. Evaluar si añadir Tavily/Perplexica para búsqueda web en OpenCode
