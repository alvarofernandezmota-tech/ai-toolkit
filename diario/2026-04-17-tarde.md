# Diario — 17 Abril 2026 (Tarde)

> Sesión: ~17:30 - 18:15 CEST
> Participantes: Álvaro + Perplexity AI (MCP GitHub)
> Estado al cierre: ✅ Sistema Colmena funcional

---

## 🎯 Objetivo de la sesión

Retomar el repo `ai-toolkit`, arreglar que OpenCode funcione con modelo local (Gemini tenía cuota agotada), y arrancar el sistema Colmena completo con las 3 pantallas tmux.

---

## ✅ Lo que hicimos

### 1. Fix `opencode.json` — modelo default
- **Problema:** `gemini-flash` tenía cuota agotada → `500 Internal Server Error`
- **Solución:** Cambiado default a `ollama/qwen2.5-coder:14b`
- **Commit:** `fix: cambiar modelo por defecto a ollama local (gemini cuota agotada)`

### 2. Fix `start-colmena.sh` — Permission denied
- **Problema:** Script sin permisos de ejecución tras clone
- **Solución:** `chmod +x scripts/start-colmena.sh`
- **Pendiente en repo:** añadir `.gitattributes` con `scripts/*.sh eol=lf` para que git mantenga permisos

### 3. Fix tmux — server exited unexpectedly
- **Problema:** Tras `wsl --shutdown`, el server tmux muere
- **Solución:** Reiniciar WSL limpio antes de arrancar colmena
- **Flujo correcto:** `wsl --shutdown` → `wsl` → `cd ~/projects/ai-toolkit` → `bash scripts/start-colmena.sh`

### 4. Sistema Colmena funcionando ✅
- 3 paneles tmux correctos:
  - **Izquierdo:** OpenCode con Qwen2.5 Coder 14B ⭐
  - **Derecha arriba:** LiteLLM logs + `200 OK`
  - **Derecha abajo:** Bash libre + health check
- LiteLLM cargando todos los modelos: ollama-coder, groq, sambanova, together, gemini (warning), claude, gpt...

### 5. Reflexión arquitectura: Core del agente
- **Insight clave:** OpenCode no tiene "core" preconfigurado como Claude Code
- Necesitamos construir nosotros el contexto, reglas y capacidades en `AGENTS.md`
- Decisión: enriquecer `AGENTS.md` con reglas de computer-mode, lista de modelos, flujo de trabajo

---

## 🐛 Bugs encontrados

| Bug | Causa | Solución |
|-----|-------|----------|
| `litellm: command not found` | No está en PATH global | Usar path completo del venv de thdora |
| `500 Internal Server Error` | Gemini sin cuota | Default → Ollama local |
| `Permission denied` en script | Git no guarda `+x` | `chmod +x` tras clone |
| tmux `server exited unexpectedly` | WSL reiniciado | Reiniciar WSL limpio antes |
| `⚠️ Ya estás dentro de tmux` | Script lanzado dentro de tmux | Salir con `Ctrl+B D` antes |

---

## 💡 Decisiones tomadas

1. **Ollama local como default** — sin cuotas, siempre disponible, suficientemente rápido
2. **AGENTS.md como "core"** — es nuestro equivalente al system prompt de Claude Code
3. **Cada prueba = 1 commit** — para tener historial real de qué funcionó y qué no
4. **Computer mode** — próxima sesión probar dar tareas complejas a OpenCode con el nuevo AGENTS.md

---

## ⏭️ Próxima sesión

1. Hacer `git pull` para bajar AGENTS.md y ROADMAP actualizados
2. Abrir OpenCode y darle una tarea real en computer mode
3. Verificar que commitea solo con `git push`
4. Construir `scripts/cerrar-sesion.sh` — cierre automático con diario
5. Atacar `thdora` con OpenCode como agente revisor

---

## 📊 Estado del sistema al cierre

```
✅ Ollama      — corriendo, 5 modelos cargados
✅ LiteLLM     — puerto 8000, todos los modelos disponibles
✅ OpenCode    — v1.4.7, modelo qwen2.5-coder:14b
✅ tmux        — sesión colmena con 3 paneles
✅ GitHub      — repo al día con todos los cambios
⚠️ Gemini      — cuota agotada (fallback disponible)
⚠️ tmux perms  — chmod +x necesario tras cada clone
```

---

*Generado por Perplexity AI MCP — sesión 17 abril 2026 tarde*
