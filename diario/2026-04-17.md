# 📅 Diario 2026-04-17

## Resumen de sesión

Primera sesión nocturna con el ecosistema 100% operativo. Se resolvió el último bloqueante (timeout Ollama) y se asignó la primera tarea autónoma real a OpenCode.

## Hitos conseguidos hoy

- [x] **Ollama local confirmado**: `qwen2.5-coder:14b` responde correctamente (curl manual ✅)
- [x] **Fix timeout**: `request_timeout` 15s → 120s — Ollama ya no cae a APIs externas
- [x] **Colmena 100% operativa**: LiteLLM `:8000` con `200 OK` estable
- [x] **OpenCode operativo**: panel "Ask anything..." activo
- [x] **Primera tarea asignada**: `scripts/generar-diario.sh` — OpenCode trabajando
- [x] **Bug documentado**: `start-colmena.sh` parsea YAML como bash — workaround documentado

## Primera tarea autónoma OpenCode (en curso 02:03)

OpenCode está ejecutando con `qwen2.5-coder:14b` (Ollama local):

```json
{
  "function_name": "task",
  "parameters": {
    "todos": [
      {"content": "Lee el archivo ROADMAP.md del repo.", "status": "pending", "priority": "high"},
      {"content": "Lee el archivo opencode.json del repo.", "status": "pending", "priority": "high"},
      {"content": "Crea scripts/generar-diario.sh", "status": "pending", "priority": "high"}
    ]
  }
}
```

Dos llamadas a LiteLLM ya completadas (`200 OK`). Procesando...

## Estado de APIs a las 02:00

| Proveedor | Estado | Razón |
|-----------|--------|-------|
| Ollama local | ✅ Operativo | Sin límites, 120s timeout |
| Groq | ⚠️ Límite TPM | 12000 tokens/min free tier superado con contextos grandes |
| Gemini Flash | ❌ Agotada | Cuota diaria free tier |
| Gemini Lite | ❌ Agotada | Cuota diaria free tier |
| Sambanova | ⚠️ Inestable | 429 frecuente |
| Together | ⚠️ Crédito limitado | $1 restante |
| OpenRouter | ⚠️ Límite variable | Funcional pero con techo |
| Cerebras | ⚠️ Límite diario | Funcional pero con techo |

## Próximas tareas para OpenCode

1. **[en curso]** `scripts/generar-diario.sh`
2. **[pendiente]** Reescribir `scripts/start-colmena.sh` (bug parseo YAML)
3. **[pendiente]** `scripts/health-check.sh` con semaforos verde/amarillo/rojo
4. **[pendiente]** Configurar repos del ecosistema (ver REPOS-ECOSISTEMA.md)

## Aprendizajes clave

- **Timeout era el único bloqueante real** — con 120s Ollama funciona perfectamente
- **El modelo tarda ~45s en cargar en RAM** la primera petición — normal, no es un error
- **Ollama > todas las APIs gratuitas** — sin límites, privado, sin coste
- **OpenCode puede trabajar solo** — primera tarea autónoma asignada y en ejecución
- **`start-colmena.sh` tiene bug crítico** — workaround documentado en INICIO-AQUI.md
