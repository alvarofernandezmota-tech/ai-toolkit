# Diario — 2026-04-22 noche

## Resumen ejecutivo

Sesión de puesta en marcha real de la colmena completa. LiteLLM arrancó correctamente con todos los modelos. Se fijó el problema de autenticación de Claude Code y se actualizó `~/.bashrc` con las keys necesarias.

---

## Qué hicimos

### 1. Claude Code con qwen2.5-coder:14b
- Intentamos arrancar Claude Code apuntando a Ollama local con el modelo `qwen2.5-coder:14b`
- Problema: conflicto de auth — `ANTHROPIC_API_KEY` y `ANTHROPIC_AUTH_TOKEN` seteados a la vez
- Solución: `unset ANTHROPIC_API_KEY` en `~/.bashrc`

### 2. LiteLLM arrancó OK
- `Uvicorn running on http://0.0.0.0:8000` confirmado en el panel derecho de tmux
- Modelos disponibles cargados: gpt-4o, llama-4-maverick, qwen3-235b, groq-fallback, ollama-fallback, etc.
- Error: health-check fallaba con `401 Unauthorized` — LiteLLM tiene auth activado pero el health-check no mandaba la API key

### 3. OPENROUTER_API_KEY fijada en ~/.bashrc
```bash
export OPENROUTER_API_KEY="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"
unset ANTHROPIC_API_KEY
```

### 4. SSH desde Windows confirmado
- IP del servidor WSL2: `10.202.77.228`
- Puerto activo: `2222` (el 22 sigue bloqueado)
- `sudo service ssh start` + `restart` ejecutados correctamente

### 5. Colmena normal arrancada (3 paneles tmux)
- Panel izquierdo: Claude Code esperando con `qwen2.5-coder:14b`
- Panel derecho arriba: LiteLLM proxy en :8000
- Panel derecho abajo: bash libre con health-check

---

## Problemas encontrados

| Problema | Causa | Estado |
|---|---|---|
| `Auth conflict` en Claude Code | Dos variables de auth seteadas a la vez | ✅ Arreglado en ~/.bashrc |
| Health-check `401 Unauthorized` | LiteLLM tiene auth pero health-check no manda key | ⚠️ Pendiente arreglar |
| `qwen2.5-coder:14b` muy lento | No cabe bien en 6GB VRAM | ✅ Script ya usa qwen3:8b por defecto |
| `--colmena-full` no arrancó | OPENROUTER_API_KEY no estaba en ~/.bashrc al lanzar | ✅ Arreglado |

---

## Estado al cerrar sesión

| Componente | Estado |
|---|---|
| LiteLLM proxy :8000 | ✅ Arrancando (health-check con 401 — fix pendiente) |
| Claude Code v2.1.117 | ✅ Operativo (auth conflict corregido) |
| OpenRouter key | ✅ En ~/.bashrc |
| SSH :2222 | ✅ Operativo |
| ~/.bashrc | ✅ Actualizado con keys y unset |
| colmena-full (2 agentes) | ⚠️ Pendiente probar mañana |

---

## Para mañana — Por aquí empezamos

### 1. Arreglar LiteLLM auth en health-check
El `litellm-config.yaml` tiene `general_settings.master_key` activo. El health-check no manda esa key. Dos opciones:
- **Opción A (fácil)**: Desactivar auth en LiteLLM quitando `master_key` del config
- **Opción B (correcta)**: Pasar `Authorization: Bearer <master_key>` en el health-check

### 2. Probar colmena-full con los dos agentes
```bash
tmux kill-session -t colmena 2>/dev/null
bash scripts/start-colmena.sh --colmena-full
```
- Panel izquierdo: Claude Code → LiteLLM → modelos
- Panel derecho: OpenCode → OpenRouter → llama-3.3-70b free

### 3. Primera sesión real en THDORA
- Ver `agentes/thdora-primera-sesion.md` para los bugs documentados
- Arrancar con: `bash scripts/start-colmena.sh --claude-thdora`

### 4. Renovar keys caducadas
- Groq: https://console.groq.com/keys
- DeepSeek: https://platform.deepseek.com
- Gemini: https://aistudio.google.com/apikey

---

## Comando de arranque mañana

```bash
cd ~/projects/ai-toolkit
source ~/.bashrc
bash scripts/start-colmena.sh --colmena-full
```
