# Diario ai-toolkit — 24 abril 2026 (noche)

## Resumen

Sesión nocturna centrada en poner thdora corriendo en el servidor Ubuntu (PC grande) con Docker. Objetivo cumplido: bot activo 24/7 con `restart: always`.

---

## Lo que se hizo

### Infraestructura servidor
- `git pull` en ai-toolkit y thdora → ambos actualizados
- Instalación Docker + docker-compose-v2 en Ubuntu
- Bot thdora corriendo en Docker con `restart: always`

### Fixes aplicados en thdora (commits de hoy)
- `requirements.txt`: eliminado `backports.asyncio.runner==1.2.0` (no existe para Python 3.12)
- `docker-compose.yml`: puerto API cambiado 8000→8001 (8000 lo usa LiteLLM de ai-toolkit)
- `docker-compose.yml`: healthcheck corregido — usa `/` en vez de `/health`
- `docker/entrypoint-bot.sh`: mismo fix — espera a `/` en vez de `/health`

### Estado final
```
✔ thdora-api   Up (healthy)   :8001
✔ thdora-bot   Up             corriendo
```

---

## Pendiente

- [ ] API key Groq inválida en `.env` del servidor → NLP no funciona (menú de botones sí)
- [ ] Instalar Tailscale para SSH remoto desde el Acer
- [ ] Script de auto-update: `git pull + docker compose up --build`
- [ ] Decidir: arreglar Groq key vs migrar NLP a Ollama local

---

## Relación con ai-toolkit

El puerto 8000 queda reservado para LiteLLM/ai-toolkit. thdora corre en 8001. Ambos servicios conviven en el mismo servidor Ubuntu sin conflicto.

Próximo paso en ai-toolkit: integrar scripts del ROADMAP (morning.sh, day-close.sh) ahora que el servidor está siempre activo.
